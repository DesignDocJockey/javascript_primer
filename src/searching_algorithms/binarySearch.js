'use strict';

/**
 * Contents of the array must be stored before it can be used
 * Performs well because it cuts the dataset we are searching through in half each time (Divide & Conquer)
 */
const array =  [ 2, 3, 4, 10, 40 ];
 const binarySearch = (array) => {


 }